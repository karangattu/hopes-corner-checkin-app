# ==============================================================================
# Hope's Corner Check-In System - Environment Variables
# ==============================================================================
# Copy this file to .env.local and fill in your values
# 
# IMPORTANT: Never commit .env.local to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# SUPABASE - Database & Authentication Backend
# ------------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Public URL for your Supabase project (safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Publishable key for client-side access (safe to expose - uses RLS)
# Get from: Dashboard > Settings > API Keys > Publishable (Default)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_...

# Secret key (KEEP SECRET - bypasses RLS, server-side only)
# Get from: Dashboard > Settings > API Keys > Secret
SUPABASE_SECRET_KEY=sb_secret_...

# ------------------------------------------------------------------------------
# NEXTAUTH - Session Management
# ------------------------------------------------------------------------------
# Used for encrypting JWT session tokens

# Secret key for signing sessions (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your_random_secret_key_here

# Base URL of your app (optional in production, auto-detected)
NEXTAUTH_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# OPTIONAL - Development Settings
# ------------------------------------------------------------------------------

# Set to 'development' or 'production'
# NODE_ENV=development

# Enable verbose logging (optional)
# DEBUG=true
